import { BoxGeometry, ConeGeometry, TorusGeometry } from 'three';

export const boxGeometry = new BoxGeometry(1, 1, 1);
export const coneGeometry = new ConeGeometry(0.5, 0.5, 3);
export const torusGeometry = new TorusGeometry(5, 2, 4, 25); 
